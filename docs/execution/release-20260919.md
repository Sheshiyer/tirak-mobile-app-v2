# Court feedback release — 19 September 2026

## Scope and source

The owner approved **all current application changes**, including the existing
payment and iOS work. This does not activate payment providers or authorize a
blanket production migration. All payment activation flags remain off.

The working checkout began at `6b37892`, 25 commits behind GitHub `main` at
`c52f4c5`. The release branch starts from that newer upstream, preserving its
payment state safeguards, scene-link lifecycle, review mode and OTA work.
The local feedback fixes were checkpointed in `ed0cb8e` and reconciled into an
isolated release worktree. Older overlapping payment-screen implementations
must not replace the newer upstream behavior.
Final remote refresh also incorporated `41550c7` (review OTA test-environment
isolation). Review/demo/payment build flags stay scoped to publishing, not tests.

## Expo delivery

- Existing project: `@thoughtseedlabs-2/tirak-companion-marketplace`.
- Project ID: `ba0d6f3e-ed4c-4aaf-8cff-aa2fdb253cc5`; no replacement project created.
- EAS CLI `24.7.0` installed; SDK-compatible local Expo CLI is used with Bun.
- Bun `1.3.5` and the upstream `bun.lock` are authoritative.
- Native Android and iOS configurations enable updates and use the same project
  URL and `file:fingerprint` runtime as the dynamic Expo configuration.
- Development, preview and production have explicit, separate channels and EAS
  environments. The isolated review profile retains review-only fixtures.
- Legacy demo mode and PromptPay are disabled in release profiles and OTA CI.
- Upload exclusions cover local environment/credential files, machine planning,
  worktrees and raw evidence. No credentials are added to source.

The most recent finished Android build found in EAS was production build 8,
version `1.5.1`, created on 18 June 2026 from `d4b8aa4`. That source predates
OTA-enabled native configuration. A compatible replacement binary must be
installed before it can receive this fingerprint's updates. Court's actual
installed version has not been inspected. An existing **review** OTA group from
17 September is not proof of delivery to a production Android installation.

## Operational gates

Both Expo production and preview environments currently target
`https://tirak-backend.tirak-court.workers.dev`. This is the **default** Worker;
checked-in Wrangler configuration binds it to `tirak-development`, not the
separately named production Worker/database. Preserve current user data and
confirm that target before any live deployment or database operation.
The live `/health` probe returned HTTP 200 with `environment: development` at
`2026-09-19T13:49:26.824Z`, confirming this is not just a local config label.

Cloudflare CLI is not authenticated. The backend release needs authenticated
schema inspection, approved booking-chat tables, additive account migration
015, and only missing customer-profile compatibility columns. Never use the
blanket migration/deploy script for this release.

The local admin remote points to a different app. The matching private source
repository is `pineappleinnovationlabs/tirak-admin-command-center`; the owner
must resolve the publication target before it is pushed. The backend's admin
CORS allowlist must match its eventual deployed hostname.

Interceptor reported `no extensions connected`. Browser rendering and installed
Android/two-user messaging checks remain outstanding. Code publication, a build
artifact, OTA publication and actual device delivery are separate milestones;
none implies that the others are complete.

## Release sequence

Integrated local verification: TypeScript passes; Jest **330 tests / 25 suites**
pass, including upstream payment/review regressions and new OTA configuration
checks. `eas build:inspect --platform android --profile preview --stage archive`
successfully creates the candidate upload archive. These are not device results.

Android preview build `e23a3193-5662-4dd5-b7a1-e0aac85d6e8b` was submitted from
source `31fa5d1` using the existing remote keystore. Its runtime is
`13d8d14e3b5eb37d6aec89fe1f0434d6a02d8f29`. The local runtime matches exactly
after Expo's native XML normalization; the normalized files are retained for
reproducible publishing. Build completion and OTA publication are recorded
separately below when confirmed.

1. Reconcile and test source; publish reviewed release branches.
2. Resolve admin/backend targets and authenticate Cloudflare.
3. Inspect and back up the exact database; apply only approved missing schema.
4. Deploy the matching backend and verify email delivery and account/chat APIs.
5. Install a matching internal Android build and verify actual user journeys.
6. Publish a verified update to the matching channel/runtime. Production OTA CI
   is restricted to `main`; do not bypass that gate from a release branch.

Use `docs/eas-update-release.md` for publishing and rollback. Record actual
commit SHAs, build IDs and update-group IDs only after each operation succeeds.
