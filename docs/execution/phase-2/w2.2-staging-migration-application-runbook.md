# W2.2 — Apply Canonical Migration Lineage to Active Staging · Execution Runbook

Status: **DRAFT FOR HUMAN REVIEW — NOT YET EXECUTED**
Prepared: 2026-07-24
Task: **No dedicated tracker issue — recommend the owner create one before execution; do not invent an issue number.**
Wave: W2.2 gate entry · Swarm: integration-gate
Owner: Data owner / backend executor (human gates embedded)
Estimate: 4 hours
Dependencies: T-031 ✅ (PR #21 merged `cf26d06e…`), **T-032 ✅ GO signed 2026-07-24 by the human release owner** — packet `docs/execution/phase-2/t-032-staging-migration-go-no-go-decision-packet.md`; canonical copy on backend PR #22, branch `codex/tirak-omise/w2.1/t-032-go-no-go-decision`, head `784aa32`
Contract: `tirak-payments-v1`
Execution repo: `Tirak/Backend/tirak-backend-alpha01` (branch from the PR #22 merge commit — see Stage 0 dependency note)
Final evidence destination: `docs/execution/phase-2/w2.2-*` + `evidence/w22/` in the backend repo

## ⚠️ No tracker issue — read first

The frozen plan's W2.2 begins at **T-033** (Omise cloud config). The staging migration
application has **no dedicated tracker issue**: it is the gate-entry action implied by
T-032's signed GO and is a **precondition of T-036** (deploy baseline Worker to staging).
**Recommend the owner create a tracking issue before execution begins; do not invent an
issue number.** If an issue exists by Stage E, the evidence comment lands there; if not,
that absence is recorded in the evidence ledger.

## ⚠️ First-write note — this is the first staging write in program history

Active `tirak-staging` (uuid `5132c8cc-8f23-4dd2-94d1-9d53edb92888`, pinned account
`2c0c96c68f0ee73b6d980054557bca5b`) has held `num_tables: 0` with **zero writes across
the entire program to date**. Every prior rehearsal ran on disposable databases
(T-026, T-029, T-030, T-031). **This runbook is the ONLY artifact authorized to write to
`tirak-staging`**, and only after its own human gates. There is no disposable DB here and
no teardown step — the write is permanent unless the BREAK-GLASS rollback is invoked.

## Authority boundary

Published under the recorded T-024 human approval and entered through the **signed T-032
GO** (2026-07-24, human release owner). The T-032 packet's own words govern this
execution:

> *"A GO authorizes the W2.2 execution to apply the canonical lineage
> (`canonical-baseline.sql` + `008_omise_promptpay_payments.sql` +
> `010_booking_chat_expansion.sql` + `011_payment_restitutions.sql`) to active
> `tirak-staging`, **subject to every pre-condition in §6 and every abort trigger
> in §7**."* (T-032 §5)

> *"The W2.2 execution operates under its own runbook and human gates; this decision is
> its entry ticket, not its execution plan."* (T-032 §5)

Prohibited under this runbook: production mutation, live Omise charging, App Store
submission, secret mutation, deploying the Worker, writing any application data, and any
write target other than `tirak-staging` itself. Restore-by-overwrite of staging remains
contract-forbidden (migration-strategy.md line 65) except under the BREAK-GLASS path
below with fresh explicit human authorization.

Two explicit human gates (same two-party discipline as T-025/T-026/T-029/T-030/T-031):

- **GATE 1 (before Stage B):** the decisive gate — authorizes the first staging write in
  program history: exactly one operation class, applying baseline + 008 + 010 + 011 to
  `tirak-staging` via isolated apply roots. Nothing else.
- **GATE 2 (before Stage E):** human review of Stage D verification results → authorize
  marking the staging migration COMPLETE and publishing evidence. No teardown exists.

## Binding pre-conditions (T-032 §6, quoted verbatim) — satisfied-at-stage mapping

T-032 §6 states these conditions are *"binding on the W2.2 execution. A GO is void as to
any execution that does not satisfy all of them."* Each is quoted and mapped to the
stage that satisfies it:

| # | Binding pre-condition (T-032 §6, verbatim) | Satisfied at |
| --- | --- | --- |
| 1 | *"Fresh recovery point immediately before apply — a full T-026-chain recovery point on `tirak-staging` (time-travel bookmark + export) captured immediately before the apply, with the T-026 export-URL redaction rule in force."* | **Stage A** |
| 2 | *"Isolated apply roots — migrations are applied from isolated roots with dedicated configs; **never** the repo `wrangler.toml` migrations root, which contains the quarantined files (`004_mobile_app_features.sql`, `009_booking_scoped_chat.sql`)."* | **Stage B** |
| 3 | *"Apply order — baseline root first, then the lineage root in filename order: 008 → 010 → 011."* | **Stage C** |
| 4 | *"Post-apply verification — the full T-031 probe set re-run against staging: contract-surface exact match (14/14 objects), `PRAGMA foreign_key_check` zero rows, ledger exactly 4 rows in canonical order with `verify-lineage.mjs` exit 0, and no quarantined filename present."* | **Stage D** (structural-only subset — see Stage D note) |
| 5 | *"Human two-party confirmation recorded at each gate — every execution gate (pre-flight, apply, post-apply verification, closure) carries a recorded human two-party confirmation, per the T-026/T-029/T-030/T-031 gate discipline."* | **Stage A mini-gate, GATE 1, Stage B target-verification, GATE 2** |

## Inputs frozen by T-028/T-029/T-030/T-031/T-032

| Artifact | Identity |
| --- | --- |
| Target | `tirak-staging`, uuid `5132c8cc-8f23-4dd2-94d1-9d53edb92888`, pinned account `2c0c96c68f0ee73b6d980054557bca5b`, currently PRISTINE (`num_tables: 0`) |
| Baseline | `migrations/baseline/canonical-baseline.sql`, sha256 `b6532c80e5eeb6b481c26f5ad12f58043f8ad77587ea503527f6cb94e47cf33f` (regeneration via `node scripts/migrations/generate-canonical-baseline.mjs` must match; drift = abort) |
| Payments | `migrations/008_omise_promptpay_payments.sql` |
| Chat | `migrations/010_booking_chat_expansion.sql` (additive; zero legacy data copy) |
| Restitutions | `migrations/011_payment_restitutions.sql` (additive sibling) |
| Apply set | Baseline + 008 + 010 + 011 — exactly these four, in this order |
| Contract surface | `contracts/tirak-payments-v1/target-schema.sql` — 14 objects |
| Expected post-apply ledger | Exactly 4 rows, canonical order: `canonical-baseline.sql`, `008_omise_promptpay_payments.sql`, `010_booking_chat_expansion.sql`, `011_payment_restitutions.sql` |
| Pre-apply fingerprint | `3b57299a2a7cadc048243a18aeae8cc6d568b548eacf3edcfaa5ddbc24eef7cc` (successor value, human-confirmed during T-029, carried through T-030/T-031) |
| Quarantined (never applied) | `004_mobile_app_features.sql`, `009_booking_scoped_chat.sql` — unreachable from the isolated apply roots |
| Ledger rule | `node scripts/migrations/verify-lineage.mjs <schema.json> <ledger.json>` — exit 0 pass, 1 refuse-and-escalate, 2 usage/IO |

## Acceptance mapping → evidence

| Acceptance | Evidence artifact |
| --- | --- |
| Canonical lineage applied to `tirak-staging` | apply transcripts (sanitized) under `evidence/w22/` |
| Schema matches contract exactly | post-apply `sqlite_schema` dump vs `target-schema.sql` (surface-diff harness, 14/14 objects) |
| Foreign keys clean | `PRAGMA foreign_key_check` → zero rows |
| Ledger exact | `d1_migrations` dump → `verify-lineage.mjs` exit 0; exactly 4 rows, canonical order, no quarantined names |
| No application data written | row-count manifest: ALL tables zero rows except `d1_migrations` (4 rows) |
| Recovery point exists before the write | Stage A Time Travel bookmark + full export + checksums under `evidence/w22/` |
| New post-apply fingerprint captured | post-apply `npm run staging:discover` fingerprint recorded and flagged for human confirmation (GATE 2) |

---

## Stage 0 — Preconditions (offline)

```bash
cd Tirak/Backend/tirak-backend-alpha01
git checkout main && git pull --ff-only origin main
# DEPENDENCY: requires PR #22 (T-032 decision packet) merged to main.
# If PR #22 is still OPEN at execution time, branch explicitly from its head instead:
#   git checkout -b codex/tirak-omise/w2.2/staging-migration-application 784aa32
# and record the branch-from decision in the ledger.
git checkout -b codex/tirak-omise/w2.2/staging-migration-application
node scripts/migrations/generate-canonical-baseline.mjs   # regenerate; hash MUST equal b6532c80…
test -f .env.tirak-staging && [ "$(stat -f '%Lp' .env.tirak-staging)" = "600" ]
```

Credentials are exported via the sed pattern only — **never echoed**:

```bash
export CLOUDFLARE_ACCOUNT_ID=$(sed -n 's/^TIRAK_CLOUDFLARE_ACCOUNT_ID=//p' .env.tirak-staging)
export CLOUDFLARE_API_TOKEN=$(sed -n 's/^TIRAK_CLOUDFLARE_API_TOKEN=//p' .env.tirak-staging)
```

Recommendation (not a requirement): the broad-token rotation residual (T-032 §8 item 3)
be settled before execution. It does not block this runbook but is recorded here.

Abort if: baseline hash drift (means 001–008 changed), credential checks fail, tree dirty,
or neither PR #22 merged nor an explicit branch-from-`784aa32` plan is recorded.

## Stage A — Fresh recovery point on active staging (read-only; T-026 chain)

Captured **immediately before apply**, per T-032 §6.1:

```bash
npm run staging:preflight && npm run staging:discover
# expected fingerprint (SUCCESSOR value, human-confirmed during T-029):
#   3b57299a2a7cadc048243a18aeae8cc6d568b548eacf3edcfaa5ddbc24eef7cc
TS=$(date -u +%Y%m%dT%H%M%SZ); mkdir -p evidence/w22
npx wrangler d1 info tirak-staging --json > evidence/w22/d1-info-pre.json
npx wrangler d1 time-travel info tirak-staging --json > evidence/w22/bookmark-pre.json
npx wrangler d1 export tirak-staging --remote --output evidence/w22/tirak-staging-pre-$TS.sql
sha256sum evidence/w22/* > evidence/w22/checksums.sha256
```

Pinned account `2c0c96c68f0ee73b6d980054557bca5b`; staging D1 `tirak-staging` uuid
`5132c8cc-8f23-4dd2-94d1-9d53edb92888`. Export-URL redaction rule (T-026) in force:
signed URLs never appear in transcripts or evidence.

**Fingerprint gate (T-032 §7 trigger 1):** the computed pre-flight fingerprint MUST equal
`3b57299a2a7cadc048243a18aeae8cc6d568b548eacf3edcfaa5ddbc24eef7cc`. PR #22 adds no
migration files, so no drift is expected. Any drift → **STOP**, preserve evidence
append-only, escalate. No mini-gate exception applies here: unlike T-030, no lineage
merge intervenes between the confirmed fingerprint and this execution.

**Pristine gate (T-032 §7 trigger 2):** `d1 info` MUST show `num_tables: 0`. Any
pre-existing table → **abort immediately**: someone else wrote to staging; escalate with
the captured evidence.

**Two-party confirmation (T-032 §6.5, pre-flight gate):** record the human confirmation
that Stage A results match expectations before proceeding to GATE 1.

## ⛔ GATE 1 — Human approval required (the decisive gate)

This gate authorizes the **first write to active staging in program history**. The
approval statement:

> "W2.2 requests authority to perform exactly one operation class: applying the
> canonical baseline and migrations 008, 010, and 011 to active `tirak-staging`
> (uuid `5132c8cc-8f23-4dd2-94d1-9d53edb92888`) via the isolated apply roots in
> Stage B. No other mutation is requested — no Worker deploy, no secret mutation,
> no Omise activity, no application data. The Stage A recovery point is captured;
> the BREAK-GLASS rollback is registered. Post-apply results return for human
> review at GATE 2. I acknowledge and adjudicate the §6.4 variance: post-apply
> verification on staging is the structural subset only, the behavioral subset
> carried as proven by T-030/T-031 evidence."

Without this explicit approval, Stage B does not begin.

## Stage B — Isolated apply roots + wrong-target double-verification

```bash
mkdir -p rehearsal/w22/migrations rehearsal/w22/migrations-baseline
cp migrations/baseline/canonical-baseline.sql rehearsal/w22/migrations-baseline/
cp migrations/008_omise_promptpay_payments.sql rehearsal/w22/migrations/
cp migrations/010_booking_chat_expansion.sql rehearsal/w22/migrations/
cp migrations/011_payment_restitutions.sql rehearsal/w22/migrations/
```

Isolated apply roots under `rehearsal/w22/`: `migrations-baseline/` holds the baseline
copy only; `migrations/` holds copies of 008, 010, 011 **only** — the quarantined
`004_mobile_app_features.sql` and `009_booking_scoped_chat.sql` are unreachable. Two
dedicated configs (**never** the repo `wrangler.toml`, whose `migrations/` root contains
the quarantined files):

- `rehearsal/w22/wrangler.baseline.toml` → `migrations_dir = "./migrations-baseline"`,
  `database_id = "5132c8cc-8f23-4dd2-94d1-9d53edb92888"` (**STAGING**, not a rehearsal DB)
- `rehearsal/w22/wrangler.lineage.toml` → `migrations_dir = "./migrations"` (008 + 010 +
  011), `database_id = "5132c8cc-8f23-4dd2-94d1-9d53edb92888"` (**STAGING**)

**Wrong-target double-verification (mandatory, before any apply):** print the resolved
database name and uuid from each config and require them to equal
`tirak-staging` / `5132c8cc-8f23-4dd2-94d1-9d53edb92888`. A human reads the printed
values aloud against this runbook (two-party confirmation, T-032 §6.5, apply gate).
Resolved uuid ≠ `5132c8cc-8f23-4dd2-94d1-9d53edb92888` → **STOP** (abort trigger 8,
below). This guards against wrong-target writes — the one irreversible error class a
fresh recovery point cannot excuse.

## Stage C — Apply (active staging)

```bash
npx wrangler d1 migrations apply tirak-staging --remote --config rehearsal/w22/wrangler.baseline.toml
npx wrangler d1 migrations apply tirak-staging --remote --config rehearsal/w22/wrangler.lineage.toml
```

Baseline root first, then the lineage root. A single lineage-root apply covering
008 → 010 → 011 is correct: wrangler applies pending migrations in filename order, and
the verifier permits flexible 010/011 sibling order after 008.

**Halt on ANY error. No retry without diagnosis.** Partial-failure evidence is preserved
append-only (T-032 §7 trigger 4).

Expected ledger afterwards (**exactly 4 rows**, canonical order):
`canonical-baseline.sql`, `008_omise_promptpay_payments.sql`,
`010_booking_chat_expansion.sql`, `011_payment_restitutions.sql`. Any deviation —
including any quarantined filename appearing in the ledger — → stop, capture, escalate
(T-032 §7 trigger 5).

## Stage D — Verification (read-only against staging)

**CONSERVATIVE, structural-only on staging.** Behavioral negatives (duplicate-attempt
guard, one-room-per-booking UNIQUE, restitution lifecycle CHECK, dual-Worker
compatibility) were already proven **twice** on disposable databases — T-030 and T-031.
Staging writes stay minimal: **migrations only, NO probe fixtures**. The T-032 §6.4
"full T-031 probe set" is therefore satisfied by its structural subset here, with the
behavioral subset carried as already-proven by T-030/T-031 evidence.

1. **Schema surface:** dump `sqlite_schema` (minus system tables) and assert exact DDL
   match vs `target-schema.sql` using the existing surface-diff harness
   (`tests/migrations/schema-surface.test.ts`, invoked `npx vitest run tests/migrations/`)
   against the live dump — the 14 objects: `payment_attempts`, `payment_webhook_events`,
   `idx_payment_attempts_customer`, `idx_payment_attempts_charge`,
   `idx_payment_webhook_events_charge`, `uq_payment_attempt_active_booking`,
   `booking_chat_rooms`, `booking_chat_messages`, `idx_booking_chat_rooms_customer`,
   `idx_booking_chat_rooms_supplier`, `idx_booking_chat_messages_room_time`,
   `payment_restitutions`, `idx_payment_restitutions_booking`,
   `idx_payment_restitutions_customer`.
2. `PRAGMA foreign_key_check;` → zero rows.
3. `d1_migrations` dump → `node scripts/migrations/verify-lineage.mjs <schema.json> <ledger.json>`
   → must exit 0 (exactly 4 rows; baseline first, then 008, then 010/011; no quarantined
   names).
4. **Row-count manifest:** ALL tables zero rows except `d1_migrations` (4 rows). Any
   other non-zero table → abort trigger: unexpected application data on staging.
5. **Post-apply fingerprint:** run `npm run staging:discover` and record the new
   fingerprint. It **WILL legitimately change** — staging now has schema. Capture the
   new fingerprint as the new baseline for future gates and flag it for human
   confirmation at GATE 2, under the same two-party discipline as T-025/T-029.

Any FK violation or contract-object mismatch → abort (T-032 §7 trigger 6).

## BREAK-GLASS — Emergency rollback (never a test path)

Restore-by-overwrite is **contract-forbidden** (migration-strategy.md line 65). This
section is the sole exception, and only under all of the following:

1. Post-apply verification (Stage D) has failed, AND
2. the human authorizes rollback **at that moment** — a fresh, explicit approval given
   in direct response to the failure, not a standing pre-authorization.

If both hold: Time Travel restore `tirak-staging` to the **Stage A bookmark**, then
re-verify pristine (`d1 info` → `num_tables: 0`; discovery fingerprint back to
`3b57299a…eef7cc`), and record everything append-only — the failure, the approval
statement verbatim, the restore transcript, and the re-verification. This is an
emergency path, never a rehearsal or test path. If rollback is not authorized, halt in
place with evidence preserved and escalate.

## ⛔ GATE 2 — Human approval required

Human review of the complete Stage D results, including the new post-apply fingerprint.
The approval statement:

> "I have reviewed the W2.2 post-apply verification: 14/14 contract objects exact,
> FK check zero rows, ledger exactly 4 rows in canonical order with verify-lineage
> exit 0, row-count manifest clean, and I confirm the new post-apply discovery
> fingerprint `<recorded-value>` as the successor baseline for future gates. I
> authorize marking the staging migration COMPLETE and publishing the evidence."

No teardown exists — there is no disposable DB. Completion is marked only by this gate.

## Stage E — Evidence closure

- `docs/execution/phase-2/w2.2-staging-migration-evidence.md` +
  `w2.2-staging-migration-ledger.json`
- `evidence/w22/` artifact bundle: pre-apply info/bookmark/export, apply transcripts
  (sanitized — signed URLs redacted per the T-026 rule), post-apply dumps, probe
  outputs, both fingerprints, `checksums.sha256`
- Scoped commit → PR to `Sheshiyer/tirak-backend-alpha01` (PR #15–#22 pattern) —
  **not merged by the executor; the human closes the PR**
- Comment on the tracking issue **if one exists by then** (see the no-issue note at the
  top); otherwise record the absence in the ledger.
- Branch: `codex/tirak-omise/w2.2/staging-migration-application`.

## Downstream

Success unblocks **T-036** (deploy baseline Worker to staging; dependencies
T-032 + T-033 + T-034). This runbook does **NOT** deploy the Worker, touch secrets,
enable Omise, or write application data. Those remain downstream tasks under their own
authorities.

## Abort triggers

The seven pre-registered triggers from **T-032 §7, quoted verbatim** — *"If any fires
during the W2.2 execution, the execution halts immediately, preserves all evidence
append-only, and does not retry without diagnosis and re-approval"* — plus one trigger
added by this runbook:

1. *"**Fingerprint drift** — apply-time pre-flight fingerprint ≠
   `3b57299a2a7cadc048243a18aeae8cc6d568b548eacf3edcfaa5ddbc24eef7cc`."* (Stage A)
2. *"**Staging no longer pristine** — `num_tables` ≠ 0 at pre-flight."* (Stage A)
3. *"**Baseline regeneration hash drift** — regenerated canonical baseline ≠
   `b6532c80e5eeb6b481c26f5ad12f58043f8ad77587ea503527f6cb94e47cf33f`."* (Stage 0)
4. *"**Any apply error or partial ledger write** — halt, preserve append-only evidence,
   no retry without diagnosis."* (Stage C)
5. *"**Ledger deviation** — any deviation from the canonical 4 rows
   (`canonical-baseline.sql`, `008_omise_promptpay_payments.sql`,
   `010_booking_chat_expansion.sql`, `011_payment_restitutions.sql`), or any quarantined
   filename (`004_mobile_app_features.sql`, `009_booking_scoped_chat.sql`)
   appearing."* (Stage C/D)
6. *"**Post-apply verification failure** — any FK violation or contract-object mismatch
   after apply."* (Stage D)
7. *"**Wrong write target** — any write target other than `tirak-staging`
   itself."* (all stages)
8. **(Added by this runbook) Resolved database uuid mismatch** — the resolved database
   uuid at the Stage B double-verification ≠ `5132c8cc-8f23-4dd2-94d1-9d53edb92888`.

Additionally: any table other than `d1_migrations` holding non-zero rows in the Stage D
row-count manifest is treated as trigger 6 (post-apply verification failure).

## Drift register

| Item | State | Disposition |
| --- | --- | --- |
| Stale "009" naming | Issue #30 title, ISA ISC-153, and the branch-worktree manifest say "009"/"additive-migration-009" while the quarantined `009_booking_scoped_chat.sql` is forbidden | Cosmetic; reconcile at wave close per T-032 §8 item 1; this runbook applies 010/011 only |
| ISA ISC-149–155 ledger | Bookkeeping still open at W2.2 kickoff | Close the ISA bookkeeping at wave close per T-032 §8 item 2; not schema drift; do not treat this runbook as closing it |
| Full-scope token rotation | REQUIRES separate secret-mutation authority; T-032 §8 item 3 | Recommended (not required) before execution; recorded in Stage 0; not mutated under this runbook |
| No tracker issue for W2.2 staging application | Frozen plan's W2.2 begins at T-033; the staging application is implied by T-032's GO and is a precondition of T-036 | Recommend the owner create a tracking issue before execution; do not invent an issue number; Stage E comments on it only if it exists by then |
| Post-apply fingerprint change | Staging will gain schema; discovery fingerprint will legitimately change from `3b57299a…eef7cc` | Expected, not drift: capture the new fingerprint in Stage D, human-confirm at GATE 2 as the successor baseline (T-025/T-029 discipline) |
