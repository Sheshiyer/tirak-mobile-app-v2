# Blocker-to-Ingestion-Control Crosswalk

Status: zero unknown enforcement locations; T-016 human acceptance pending

| Root blocker | Earliest enforcement | Owner | Negative test | Evidence producer | Later closure |
| --- | --- | --- | --- | --- | --- |
| multi-repository edits lacked immutable provenance | T-008 recovery manifest and executable verifier | release planner | undeclared file, tree, diff, evidence, graph, or exclusion drift makes `release:verify-baseline` nonzero | `npm run release:verify-baseline` | T-024 rechecks immutable integration baseline |
| legacy `004` encodes a schema lineage incompatible with `001` | T-011 target-schema contract; T-012 target-ledger strategy | data owner | disposable schema introspection plus strategy test rejects raw `004` replay and unknown target | backend `tests/contracts/payment-contract.test.ts` | T-021 fail-closed migration gate; T-028 rehearsed implementation |
| legacy `009` renames chat tables destructively | T-012 additive expand/contract strategy | data owner | strategy test requires `booking_chat_rooms` alongside legacy tables and marks destructive `009` forbidden | backend contract test and dependency graph | T-021 gate; T-028 dual-Worker implementation |
| deploy/backup commands can report false success or target stale resources | T-013 disabled-by-default environment contract | infrastructure owner | invalid/missing mode, secret mismatch, live/test crossover, and unresolved external targets all refuse creation/deployability | table-driven runtime tests and environment matrix | T-021 command/restore failure matrix; T-025 identity resolution |
| seed/import/cache surfaces can reintroduce companionship categories | T-015 provenance contract and source deny tests | product/content owner | SQL seed and mobile source scans reject named companionship/adult/direct-cash fixtures; local paid fixture is forbidden | backend contract test plus mobile `release-provenance.test.ts` | T-020 built-output scan; T-044/T-045 live data cleansing |
| booking cancellation and payment settlement were split, allowing false refund or late payment | T-010 state matrix and cancellation interlock; T-014 mobile normalization | backend and mobile owners | 24 backend contract cases, cancellation race route tests, legacy `refunded` downgrade, unversioned/ambiguous mobile response rejection | backend 186-test suite plus mobile 47-test suite | T-038 adversarial staging matrix; T-043 restitution operations |
| PromptPay completion could be trusted from mobile or webhook payload alone | T-009 owned status/recovery routes; T-014 strict mobile contract | backend and mobile owners | owner mismatch, metadata/amount/source mismatch, forged/stale/replayed webhook, and self-declared mobile paid state fail | focused Omise route tests and mobile API tests | T-037 live staging happy path; T-041 scheduled reconciliation |
| paid App Reviewer fixture could be manufactured locally | T-015 fixture-provenance specification | human release owner | seeded/manual paid status, fake charge id, mocked provider success, and serializer-only paid value are prohibited | provenance JSON plus seed/source tests | T-076 one legitimate small production charge with provider receipt |

Every blocker has an earliest control, accountable owner, executable or predeclared negative proof, evidence producer, and later task that closes the live operational risk. No blocker is delegated to UI hiding or release-note prose.
