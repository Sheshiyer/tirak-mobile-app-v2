---
phase: 01-local-promptpay-pending-checkout
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - constants/payment-capabilities.ts
  - app/api/booking/booking.ts
  - components/booking/BookingWizard.tsx
  - components/booking/steps/BookingSummaryStep.tsx
  - components/booking/steps/PaymentSelectionStep.tsx
  - components/booking/steps/BookingConfirmationStep.tsx
  - components/booking/payment/PromptPayPendingCard.tsx
  - locales/en.json
  - locales/th.json
  - scripts/promptpay-contract-mock.mjs
  - __tests__/payment-capabilities.test.ts
  - __tests__/PromptPayCheckout.test.tsx
  - __tests__/BookingWizardPaymentFlow.test.tsx
autonomous: false
requirements: [CAP-01, CASH-01, CASH-02, FLOW-01, FLOW-02, UI-01, UI-02, UI-03, A11Y-01, TEST-02, TEST-03]
user_setup: []
must_haves:
  truths:
    - "D-01: Only Tirak traveler booking files change; Tirak Plus and supplier signup remain untouched."
    - "D-02: Cash stays visible in every PromptPay state."
    - "D-03: PromptPay renders only for the explicit loopback local-test capability."
    - "D-04: The successful booking response is stored before payment selection and must be confirmed for charge creation."
    - "D-06: Pending UI renders server charge fields rather than client-calculated provider values."
    - "D-07: QR rendering is visibly pending and never paid."
    - "D-08: Cash and PromptPay use distinct truthful copy."
    - "D-09: Loading prevents duplicate booking and charge actions."
    - "D-10: Cash and pending-QR journeys are inspected on iOS 27 Simulator with a non-provider local mock."
    - "D-11: Cash and method switching lock whenever a PromptPay charge may exist."
    - "D-12: Step 6 renders payment selection and Step 7 renders booking/payment confirmation."
    - "D-13: Nullable QR, charge ID, or expiry fields render a locked truthful state without broken or fabricated content."
  artifacts:
    - path: "constants/payment-capabilities.ts"
      provides: "Fail-closed local PromptPay visibility predicate"
      exports: ["isLocalPromptPayEnabled"]
    - path: "components/booking/steps/PaymentSelectionStep.tsx"
      provides: "Cash-first, capability-gated traveler payment selection and charge action"
      contains: "PromptPay"
    - path: "components/booking/payment/PromptPayPendingCard.tsx"
      provides: "Accessible server-authoritative pending QR presentation"
      contains: "Payment pending"
    - path: "components/booking/steps/BookingConfirmationStep.tsx"
      provides: "Separate truthful booking and payment confirmation states"
    - path: "components/booking/BookingWizard.tsx"
      provides: "Reachable seven-step summary to payment to confirmation route"
      contains: "PaymentSelectionStep"
    - path: "scripts/promptpay-contract-mock.mjs"
      provides: "Disposable loopback contract fixture that never calls Omise"
      contains: "127.0.0.1"
    - path: "__tests__/PromptPayCheckout.test.tsx"
      provides: "Traveler-state and copy regressions"
    - path: "__tests__/BookingWizardPaymentFlow.test.tsx"
      provides: "Summary to payment to confirmation navigation regression"
  key_links:
    - from: "components/booking/steps/BookingSummaryStep.tsx"
      to: "stores/payment-store.ts"
      via: "setBooking before onNext"
      pattern: "setBooking"
    - from: "components/booking/steps/PaymentSelectionStep.tsx"
      to: "stores/payment-store.ts"
      via: "selected method and createCharge"
      pattern: "createCharge"
    - from: "components/booking/steps/PaymentSelectionStep.tsx"
      to: "components/booking/payment/PromptPayPendingCard.tsx"
      via: "server charge rendering"
      pattern: "PromptPayPendingCard"
    - from: "components/booking/steps/BookingConfirmationStep.tsx"
      to: "stores/payment-store.ts"
      via: "booking/payment status selectors"
      pattern: "usePaymentStore"
    - from: "components/booking/BookingWizard.tsx"
      to: "components/booking/steps/PaymentSelectionStep.tsx"
      via: "case 6 render route"
      pattern: "case 6"
---

<objective>
Deliver and visibly verify the local cash/PromptPay pending checkout slice.

Purpose: Make the approved design real in the Tirak traveler booking flow without claiming provider or staging proof.
Output: Fail-closed capability logic, method/status-aware UI, a disposable non-provider mock, automated regressions, and iOS Simulator visual evidence.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<execution_gate>
Execution authorization was received on 2026-08-31 for Tirak-mobile Phase 1 source changes and Git commits. This gate authorizes only the files and local verification in this plan; Tirak Plus, backend mutation, provider/staging activity, runtime installation, merge, deploy, and release remain held.
</execution_gate>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-promptpay-pending-checkout/01-CONTEXT.md
@.planning/phases/01-local-promptpay-pending-checkout/01-UI-SPEC.md
@.planning/phases/01-local-promptpay-pending-checkout/01-01-SUMMARY.md
@../backend/tirak-backend-alpha01/contracts/tirak-payments-v1/payment-api.json
@../backend/tirak-backend-alpha01/contracts/tirak-payments-v1/environment-matrix.json
@components/booking/BookingWizard.tsx
@components/booking/steps/BookingSummaryStep.tsx
@components/booking/steps/PaymentSelectionStep.tsx
@components/booking/steps/BookingConfirmationStep.tsx
@components/booking/BookingStepFooter.tsx
@app/api/booking/booking.ts
@app/api/payment/payment.ts
@stores/booking-store.ts
@stores/payment-store.ts
@constants/api.ts
@constants/design-tokens.ts
@locales/en.json
@locales/th.json
</context>

<threat_model>
Assets: booking ownership/status, Bearer token, charge ID, QR URL, traveler trust in paid/pending labels.
Trust boundaries: environment flags -> capability predicate; booking response -> persisted eligibility; payment response -> rendered state; local mock -> simulator evidence.
Threats: feature flag leaking to staging/production, unconfirmed booking charge, duplicate request, cash regression, fake paid claim, mock accidentally calling a provider, QR/secret capture, and cross-user stale state.
Mitigations: exact loopback plus explicit flag plus development checks, server booking guard, disabled in-flight controls, cash-first invariant, pending-only Phase 1 UI, loopback-bound mock with no network egress, redacted evidence, and reset on booking/user change.
Security gate: any non-loopback visibility, provider network call in the mock, real/staging charge, secret/QR payload capture, or local paid transition blocks completion.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write failing capability and traveler-state tests</name>
  <files>__tests__/payment-capabilities.test.ts, __tests__/PromptPayCheckout.test.tsx, __tests__/BookingWizardPaymentFlow.test.tsx</files>
  <read_first>.planning/phases/01-local-promptpay-pending-checkout/01-UI-SPEC.md, constants/api.ts, constants/design-tokens.ts, components/booking/BookingWizard.tsx, components/booking/steps/BookingSummaryStep.tsx, components/booking/steps/PaymentSelectionStep.tsx, components/booking/steps/BookingConfirmationStep.tsx, stores/payment-store.ts, locales/en.json, locales/th.json, jest.config.js, jest.setup.js</read_first>
  <action>Before UI implementation, add pure predicate tests for all combinations of flag and API URL: only exact flag `true` plus `http://localhost:8787` or `http://127.0.0.1:8787` returns true; HTTPS, non-loopback hosts, other ports, missing/mixed-case flags, and production mode return false. Add component/state behavior tests that assert cash is always rendered; cash is selectable before a request and after a definite no-charge error; cash plus all method switching are disabled for creating, in-progress, pending, indeterminate, network-unknown, and unknown states; PromptPay is absent when capability is false; PromptPay is disabled with the exact confirmed-booking helper when booking status is pending; a confirmed booking exposes `Create PromptPay QR`; two presses while creating call `createCharge` once; a complete pending response renders `Payment pending`, server `displayTotalThb`, `currency`, expiry, and the QR label; nullable `qrCodeUrl` does not render a broken image and nullable `chargeId`/omitted `expiresAt` do not render fabricated values; no pending screen contains `Paid`, `Payment complete`, or the cash-only phrase; confirmation copy distinguishes pending booking, confirmed booking, cash, and PromptPay pending. Add a wizard integration test that begins on summary, observes the stored successful booking before navigation, proves the next rendered step is `PaymentSelectionStep`, and then proves the following step is `BookingConfirmationStep`; the current skipped-payment behavior must be its initial RED failure. Mock network/store boundaries, not internal render helpers. Run the focused tests and retain the expected missing-feature assertions as RED evidence; commit only the Phase 1 test files under the authorized GSD task protocol.</action>
  <verify>npx jest __tests__/payment-capabilities.test.ts __tests__/PromptPayCheckout.test.tsx __tests__/BookingWizardPaymentFlow.test.tsx --runInBand exits non-zero on the absent capability/UI/routing behavior, not on Jest configuration.</verify>
  <acceptance_criteria>
    - Predicate tests include loopback true cases and at least five fail-closed cases.
    - UI tests assert cash presence in capability-off, unconfirmed, creating, error, uncertain, and pending cases.
    - UI tests assert cash is disabled for every state where a PromptPay charge may exist.
    - UI tests assert the exact ineligible, loading, safe-error, and pending copy from `01-UI-SPEC.md`.
    - UI tests reject paid/success language in a pending state.
    - Wizard test proves summary -> Step 6 payment -> Step 7 confirmation and catches the current missing `case 7` route.
    - RED failure is caused by absent intended behavior.
  </acceptance_criteria>
  <done>The approved UI and safety invariants exist as failing behavior tests.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate booking truth, capability-gated methods, pending QR, and confirmation copy</name>
  <files>constants/payment-capabilities.ts, app/api/booking/booking.ts, components/booking/BookingWizard.tsx, components/booking/steps/BookingSummaryStep.tsx, components/booking/steps/PaymentSelectionStep.tsx, components/booking/steps/BookingConfirmationStep.tsx, components/booking/payment/PromptPayPendingCard.tsx, locales/en.json, locales/th.json, scripts/promptpay-contract-mock.mjs</files>
  <read_first>__tests__/payment-capabilities.test.ts, __tests__/PromptPayCheckout.test.tsx, .planning/phases/01-local-promptpay-pending-checkout/01-UI-SPEC.md, ../backend/tirak-backend-alpha01/contracts/tirak-payments-v1/payment-api.json, ../backend/tirak-backend-alpha01/contracts/tirak-payments-v1/environment-matrix.json, app/api/booking/booking.ts, app/api/payment/payment.ts, stores/booking-store.ts, stores/payment-store.ts, components/booking/BookingWizard.tsx, components/booking/BookingStepFooter.tsx, components/booking/steps/BookingSummaryStep.tsx, components/booking/steps/PaymentSelectionStep.tsx, components/booking/steps/BookingConfirmationStep.tsx, constants/api.ts, constants/design-tokens.ts, locales/en.json, locales/th.json</read_first>
  <action>Implement `isLocalPromptPayEnabled({ flag, apiBaseUrl, isDev })` in `constants/payment-capabilities.ts` as a pure fail-closed predicate: require `isDev === true`, exact flag `true`, HTTP scheme, loopback host `localhost` or `127.0.0.1`, and port `8787`. Repair `BookingWizard.renderStep`: case 6 must render `PaymentSelectionStep`, case 7 must render `BookingConfirmationStep`, and no label may point to an unreachable step. In `BookingSummaryStep`, replace the cash-only pricing note with booking-review copy, keep its existing duplicate-submit guard, and call payment store `setBooking` with the returned `id`, `status`, and `paymentStatus` before `onNext()`. Preserve existing analytics but do not add charge payload/QR fields. In `PaymentSelectionStep`, keep cash first and always visible; add PromptPay only when the predicate passes; disable it for non-confirmed bookings with the exact helper text; use payment store selection/phase/error; call `createCharge()` only from the `Create PromptPay QR` action; disable all method switching while creating or whenever `errorKind` is `in-progress`, `indeterminate`, `network`, or `unknown`, and while a charge is pending; re-enable cash only for a definite no-charge error; render safe error/retry or the exact uncertain-outcome copy; render `PromptPayPendingCard` from the server charge. The pending card must use semantic tokens, server `displayTotalThb` and `currency`, `Payment pending`, and the exact server-confirmation body. Render the 224-point accessible QR image, formatted expiry, and shortened charge reference only when their respective nullable/optional server fields exist; otherwise show a locked pending/uncertain explanation without a broken image, fake identity, fabricated expiry, or local paid action. In `BookingConfirmationStep`, remove universal booking/payment success assumptions: choose booking request vs confirmed heading from stored booking status and cash vs PromptPay pending copy from selected method/charge; suppress the success icon for PromptPay pending. Its existing `resetBooking()` exit path must clear payment state through Plan 01-01's reset wiring. Add exact English strings and faithful Thai strings under payment-specific namespaces. For simulator-only proof, add a loopback-bound `scripts/promptpay-contract-mock.mjs` that accepts only `POST /api/payments/charges`, rejects any body key beyond `bookingId` and `method`, returns `{ success: true, data: { contractVersion: 'tirak-payments-v1', chargeId, paymentStatus, attemptStatus, qrCodeUrl, amountSatang, displayTotalThb, currency, expiresAt }, message: 'PromptPay charge created' }` with a non-scannable local test image, makes no outbound requests, and exits on any provider-looking environment variable. Extend the existing explicit demo-booking fallback so only `__DEV__` plus the local capability plus a test companion yields a synthetic `confirmed` booking; all non-test behavior is unchanged.</action>
  <verify>npx jest __tests__/payment-capabilities.test.ts __tests__/PromptPayCheckout.test.tsx __tests__/BookingWizardPaymentFlow.test.tsx __tests__/payment-api.test.ts __tests__/payment-store.test.ts __tests__/payment-session-reset.test.ts --runInBand && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `constants/payment-capabilities.ts` returns false for every non-loopback or non-development input.
    - `PaymentSelectionStep.tsx` renders cash outside the PromptPay capability conditional.
    - The PromptPay request handler has no amount or currency argument.
    - `BookingSummaryStep.tsx` calls `setBooking` before `onNext()` after a successful response.
    - `BookingWizard.tsx` renders `PaymentSelectionStep` for case 6 and `BookingConfirmationStep` for case 7.
    - `PromptPayPendingCard.tsx` renders only the server charge amount/currency/expiry and contains the exact pending body.
    - A null QR URL never reaches an `Image` source, and missing charge ID/expiry never produces invented display text.
    - `BookingConfirmationStep.tsx` does not render a success icon or paid copy for PromptPay pending.
    - English and Thai locale JSON parse successfully and contain distinct cash, booking, and PromptPay-pending keys.
    - The mock binds only loopback, rejects extra request fields, contains no Omise hostname/SDK call, and has no outbound HTTP client call.
    - The mock's success response uses the backend `{ success: true, data, message }` envelope.
    - Focused tests and TypeScript check exit 0.
  </acceptance_criteria>
  <done>The traveler slice satisfies the UI contract under tests and the mock cannot create a provider charge.</done>
</task>

<task type="auto">
  <name>Task 3: Build and inspect the local iOS Simulator journeys</name>
  <files>.planning/phases/01-local-promptpay-pending-checkout/evidence/01-02-simulator.md</files>
  <read_first>.planning/phases/01-local-promptpay-pending-checkout/01-UI-SPEC.md, scripts/promptpay-contract-mock.mjs, package.json, constants/api.ts, ios/Podfile, ios/Podfile.lock</read_first>
  <action>Run the full Jest suite and TypeScript check. Start only the disposable loopback mock on `127.0.0.1:8787`; verify its process has no outbound connection and its request log contains request keys but no Authorization value or QR payload. Build the existing `tirak` iOS workspace for `iPhone 17 Pro, OS 27.0` with `CODE_SIGNING_ALLOWED=NO`, launch the app with `EXPO_PUBLIC_API_URL=http://127.0.0.1:8787` and `EXPO_PUBLIC_PROMPTPAY_ENABLED=true`, and use the existing explicit test companion/demo flow. Inspect and capture screenshots for UI-SPEC scenarios 1 through 7, including a second capability-off run and large Dynamic Type. Store only redacted screenshot paths and observations in `evidence/01-02-simulator.md`; do not store a real/scannable QR, token, secret, Authorization header, or provider response. Label this as local fixture and simulator UI proof, not Omise/provider, staging, or end-to-end payment proof. Do not modify the pre-existing `ios/Podfile` or `ios/Podfile.lock` diffs; commit only the authorized Phase 1 evidence after the human checkpoint is approved.</action>
  <verify>npm test -- --runInBand && npx tsc --noEmit && xcodebuild -workspace ios/tirak.xcworkspace -scheme Tirak -configuration Debug -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=27.0' CODE_SIGNING_ALLOWED=NO build</verify>
  <acceptance_criteria>
    - Full Jest and TypeScript checks exit 0.
    - `xcodebuild` exits 0 for iPhone 17 Pro on iOS 27.0.
    - Evidence records all seven UI-SPEC scenarios with absolute screenshot paths and observed copy/state.
    - Capability-off evidence shows cash only; unconfirmed evidence shows cash plus disabled PromptPay; confirmed evidence shows the pending QR journey with cash visible but disabled after charge creation.
    - Pending evidence contains no `Paid` or payment-complete claim.
    - Large Dynamic Type evidence shows no clipped required payment/status text.
    - Evidence explicitly says no real/staging/provider charge was made.
    - `ios/Podfile` and `ios/Podfile.lock` retain their pre-plan user-owned diffs unchanged.
  </acceptance_criteria>
  <done>Automated checks, iOS build, and redacted simulator inspection prove the local UI slice at the approved evidence level.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Approve the redacted simulator evidence</name>
  <files>.planning/phases/01-local-promptpay-pending-checkout/evidence/01-02-simulator.md</files>
  <read_first>.planning/phases/01-local-promptpay-pending-checkout/01-UI-SPEC.md, .planning/phases/01-local-promptpay-pending-checkout/evidence/01-02-simulator.md</read_first>
  <action>Present the seven redacted screenshot links and their recorded state/copy observations for product-owner review. Do not continue past this gate until approval or corrective feedback is received.</action>
  <verify>The product owner responds with `approved` or a concrete visual/copy issue.</verify>
  <acceptance_criteria>
    - The evidence file links seven labeled simulator scenarios.
    - The product owner explicitly approves or identifies a visual/copy defect.
  </acceptance_criteria>
  <what-built>Tirak traveler cash and local PromptPay pending-checkout UI, verified on iPhone 17 Pro iOS 27.0 with a non-provider loopback fixture.</what-built>
  <how-to-verify>Review the seven redacted simulator screenshots linked from `.planning/phases/01-local-promptpay-pending-checkout/evidence/01-02-simulator.md`. Confirm cash remains available, PromptPay is clearly local-test and eligibility-gated, the QR state says payment pending, no paid/success claim appears, and large text remains readable.</how-to-verify>
  <resume-signal>Type "approved" or describe the visual/copy issue.</resume-signal>
  <done>The product owner approves the simulator evidence or execution returns to implementation with the described issue.</done>
</task>

</tasks>

<verification>
- [ ] Focused capability/UI tests demonstrate RED then GREEN for the intended behavior.
- [ ] Full Jest suite and `npx tsc --noEmit` exit 0.
- [ ] iPhone 17 Pro iOS 27.0 build exits 0.
- [ ] Seven simulator scenarios match `01-UI-SPEC.md`.
- [ ] Wizard integration test proves summary -> payment -> confirmation reachability.
- [ ] Cash is never hidden by PromptPay state.
- [ ] No provider, staging, or real payment operation occurred.
- [ ] Human visual checkpoint is approved.
</verification>

<success_criteria>
- All tasks and the human visual checkpoint complete.
- Every Phase 1 UI/local-proof requirement assigned to this plan is evidenced.
- The working cash checkout remains intact.
- PromptPay is fail-closed outside the explicit local capability.
- Booking and payment statuses remain separate and truthful.
- No Tirak Plus, supplier, admin, companion, backend-contract, provider, staging, production, deployment, merge, release, or iOS dependency mutation occurred; Git commits contain only authorized Phase 1 work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-promptpay-pending-checkout/01-02-SUMMARY.md` with test/build receipts, screenshot index, redaction statement, and explicit local-fixture-only epistemic status.
</output>
